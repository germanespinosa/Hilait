# Third-party notices

The bundled terminal uses [xterm.js](https://github.com/xtermjs/xterm.js) and the Fit, Search, Unicode 11, and Unicode Graphemes addons. These packages are MIT licensed. Their copyright and license notices are retained in the `licenses` directory.

Python dependencies are installed by pip and keep their respective upstream licenses. See `pyproject.toml` for the dependency list.
